# 🧩 Zookeeper Deployment and Service
apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: chatverse
spec:
  ports:
    - port: 2181
      targetPort: 2181
  selector:
    app: zookeeper
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zookeeper
  namespace: chatverse
spec:
  serviceName: "zookeeper"
  replicas: 1
  selector:
    matchLabels:
      app: zookeeper
  template:
    metadata:
      labels:
        app: zookeeper
    spec:
      containers:
        - name: zookeeper
          image: bitnami/zookeeper:3.9.1
          ports:
            - containerPort: 2181
          env:
            - name: ALLOW_ANONYMOUS_LOGIN
              value: "yes"  # В проде желательно задать аутентификацию
          resources:
            requests:
              memory: "256Mi"  # 🔧 Минимальный объём памяти
              cpu: "100m"      # 🔧 Минимальный CPU
            limits:
              memory: "512Mi"  # 🔒 Лимит памяти
              cpu: "250m"      # 🔒 Лимит CPU
          volumeMounts:
            - name: zookeeper-storage
              mountPath: /bitnami/zookeeper
  volumeClaimTemplates:
    - metadata:
        name: zookeeper-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 1Gi  # Размер хранилища можно увеличить